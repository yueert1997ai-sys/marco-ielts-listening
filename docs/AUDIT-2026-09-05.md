# 学习系统自检 · 2026-09-05

## 范围与基准

以 `origin/main` 的 `0db8f71` / Listening v2.18.0 为基准。工作前 main 干净；fetch 后建立 `codex/learning-system-audit-20260905`，未覆盖旧分支。本轮只审计和本地修复，不发布。

真实 Chrome 隔离配置，localhost:4173，390×844 与 1440×900；不读取或修改手机的个人进度。不把 Chromium 视作 iOS Safari 的替代证明。后台运行单元/词库验证，未进入生产账号修改内容。

## 当前产品状态

- 无构建框架的静态 PWA，`app.js` 管理状态与页面，普通词库每页 20 条。
- 823 条词卡：听写 276、识义 604；192 条个人同步源记录。`isRealError` 标记的源词共 236 条，与个人同步源不是同一概念。
- `source/feishu_listening.json`、`source/custom_words.json`、`source/vocabulary_overrides.json` 是词库真相源；`data/listening.json` 是生成物。
- 两条每日词汇主线：背新词 25，背错词最多 18。另保留 25 听写+25 识义旧主线、听力复习、同步词专项、重点专项、方位检测。
- `marcoIeltsListening.v1` LocalStorage：`progress[id:mode]` 记录训练；`errorWords[id]` 记录错误档案；`starred[id]` 为独立重点维度；各每日 session 保存队列和进度。未使用 IndexedDB。
- Cloudflare Worker+D1 管词库内容，不同步个人进度。易混词独立 `marcoIeltsConfusions.v1`，32 组 / 84 词。

## 已正常工作的功能

- 两条词汇主线首屏只有英文和认识/不认识；点击后才出现中文及档案。
- 浏览器实际完成新词 25 题、错词 18 题；暂停、返回、普通刷新可保存已答记录。
- 答对不自动移除活跃错词；赦免保留 wrongCount、来源、时间、已有重点状态；再错可恢复。
- 普通浏览可直接读释义，不受训练遮义限制；每页 20 条且可标重点。
- 手机/桌面首页、手机档案与浏览页无横向溢出。
- 基准验证通过：110 项主逻辑、12 项易混逻辑、12 项词库管理 Python、4 项文档结构、14 项后台 Node 测试；主词库构建与发布验证、易混构建与验证通过。

## Bug 分级（修复前）

### P0

未发现全局不可用或无条件丢失全部进度的 P0；不据此承诺不存在其他缺陷。

### P1

1. **失败导入污染内存状态**：导入带缺中文自定义词的备份，提示无效；随后进入词库点重点，旧 progress 被覆盖。赋值发生在校验和构建之前。
2. **每日错词无限补题**：完成 18 题后刷新，显示仍为 18/18，但 baseKeys 全换了 18 条，queue 又有 18 条；完成记录与分母已不属于同一批词。
3. **错词永久身份被赦免清除**：`pardonErrorWord` 与 sanitizer 把 isErrorWord 设 false。历史计数没删，但身份和活跃状态混在一起。
4. **旧错误档案迁移不完整**：给普通词旧 progress 写入 lapses=3 后刷新，errorWords 中仍不存在它；静态真实错词中非个人同步来源也未完整播种。
5. **重点入口缺失**：个人错词档案 193 张卡片无任何重点按钮；已赦免分组同样没有。揭义页入口目前靠额外 MutationObserver 补丁恢复。
6. **错词口径不一致**：“全部错词”排除了已赦免；普通词库“我的错词”按静态 isRealError，档案按 errorWords；复习池从 progress 去模式后去重，但未去除 :vocab 后缀。
7. **同日强化可虚增阶段**：词汇主线每次“认识”直接 scheduleReview；重开错词入口自动重置整轮，无到期限制。多次同日认识可一路抬升 SRS 阶段。

### P2 / 后续优化

- 错词档案缺搜索、A/B/活跃明确分组、首次错误和最近训练展示；一次渲染约 200 张卡。
- S 级规则含永久 wrongCount≥2 或 causedIeltsError，已稳定掌握的历史高错词无法降级。未来到期词仍会补入每日额度。
- 新词汇主线不提供四选一与 8–12 题回炉，旧识义主线提供。此次最新定义是两阶段自评，先保留这个产品定义，不擅自扩展题型。
- 首页大圆环是旧 50 题任务，不是两条词汇主线总和；缺清晰统计口径说明。
- 缺一般词库搜索；没有 Bottom Navigation/Modal（本轮不新增）。原生页面返回没有专用 history 路由。
- 本地存储满额/受限、坏 JSON、导入结构不完整缺保护；需与导入可靠性一起补测。多标签并发写、跨设备进度仍是已知边界。

## 数据与代码问题

错误身份、当前活跃状态、mastery、重点已分在不同对象/字段，保留此模型即可。无需重建数据库。主要修复字段语义、迁移和消费者口径。

核心脚本约 3000 行，两套 recordAttempt；部分 session 统计/同步重复；额外重点 DOM 补丁；README/HANDOFF 含旧版本和“未发布候选”描述，旧浏览器脚本硬编码 v2.17。优先修行为，不为拆文件做重构。

## 学习效率与最值得做的五件事

1. 保护导入/保存：先完整验证候选数据，失败不改内存或磁盘；保存失败明确提示并允许导出。
2. 永久错词档案：赦免只改活跃状态，迁移旧错误，不删来源和重点。
3. 固定当日任务：刷新/返回不补题；进度仅统计同一批 baseKeys；跨天重新排期。
4. 统一重点、筛选、数量：同一份档案衍生全部/活跃/赦免/S-A-B，复用分页降低长列表负担。
5. 同日认识不反复升阶段，到期再推进；难词提权但熟词允许降低频率。

## 修复与回归记录

后续独立提交在此补充；基准发现保留作为复现记录，不冒充当前未修状态。

- v2.18.1：导入失败后旧磁盘/内存均保留；旧备份刷新不重置；保存失败提示、重试和坏 JSON 恢复，390×844 浏览器 6 组回归通过。逻辑 112 项通过。缓存名称随 app version 生成，移除验证脚本固定 v35 的历史假设。
- v2.18.2：修复永久身份、迁移与多页统计；档案支持搜索/分页/S-A-B/活跃/赦免/重点，答题及揭义入口均能标重点。三种尺寸浏览器通过（236 总档案→赦免一词后 235 活跃+1 赦免，历史和重点均保留），逻辑 115 项通过。删除的揭义 DOM 补丁可通过 Git 历史恢复。
- v2.18.3：18/18 完成后刷新题单不变、队列为空；加练一词后主线仍 18/18，加练剩 17 且刷新可续练；新词完整 25/25、连续点击只记一次、双入口进度同步。119 项逻辑测试通过，修复 P1 每日补题与进度错位。
